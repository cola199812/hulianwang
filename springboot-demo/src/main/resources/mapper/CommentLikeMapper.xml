<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.outdoor.demo.mapper.CommentLikeMapper">
    <select id="countByCommentId" parameterType="long" resultType="int">
        SELECT COUNT(*) FROM comment_like WHERE comment_id = #{commentId}
    </select>
    <select id="exists" resultType="int">
        SELECT COUNT(*) FROM comment_like WHERE comment_id = #{commentId} AND user_id = #{userId}
    </select>
    <insert id="insert">
        INSERT INTO comment_like (comment_id, user_id, create_time) VALUES (#{commentId}, #{userId}, NOW())
    </insert>
    <delete id="delete">
        DELETE FROM comment_like WHERE comment_id = #{commentId} AND user_id = #{userId}
    </delete>
    <delete id="deleteByPostId" parameterType="long">
        DELETE FROM comment_like WHERE comment_id IN (SELECT id FROM comment WHERE post_id = #{postId})
    </delete>
</mapper>
