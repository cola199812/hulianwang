<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.outdoor.demo.mapper.CommentMapper">
    <resultMap id="CommentMap" type="com.outdoor.demo.entity.Comment">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.outdoor.demo.entity.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (post_id, user_id, parent_id, content, create_time)
        VALUES (#{postId}, #{userId}, #{parentId}, #{content}, NOW())
    </insert>

    <select id="findByPostId" parameterType="long" resultMap="CommentMap">
        SELECT id, post_id, user_id, parent_id, content, create_time
        FROM comment
        WHERE post_id = #{postId}
        ORDER BY create_time ASC
    </select>

    <select id="countByPostId" parameterType="long" resultType="int">
        SELECT COUNT(*) FROM comment WHERE post_id = #{postId}
    </select>
    
    <delete id="deleteByPostId" parameterType="long">
        DELETE FROM comment WHERE post_id = #{postId}
    </delete>
</mapper>
