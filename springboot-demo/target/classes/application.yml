server:
  # 服务端口号，启动后可以通过 http://localhost:8080 访问
  port: 8080

spring:
  datasource:
    # 数据库连接URL，包含数据库名、时区、字符集等配置
    url: jdbc:mysql://localhost:3306/outdoor_demo?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
    # 数据库用户名
    username: root
    # 数据库密码
    password: Azyl200508
    # 数据库驱动类名
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      # 总是初始化数据库结构和数据
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql
  jackson:
    # JSON日期格式化配置
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: UTC
  mail:
    # 邮件服务器配置（这里使用的是QQ邮箱SMTP服务）
    host: smtp.qq.com
    port: 587
    username: 2027681066@qq.com # 发件人邮箱账号
    password: ymtvgtxbzqeqejic # QQ邮箱授权码（不是QQ密码）
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

mybatis:
  # MyBatis Mapper XML文件位置（如果有XML配置的话）
  mapper-locations: classpath:mapper/*.xml
  configuration:
    # 开启驼峰命名自动映射（如数据库 user_name -> Java属性 userName）
    map-underscore-to-camel-case: true
